# 영어 내신 문제 도구

중등 영어 내신 대비 문제지를 만드는 개인용 웹 도구. 프로그램 설치 없이 브라우저로 연다.

## 여는 법

- **아무 컴퓨터에서나**: https://mycatsara.github.io/engquiz/ (즐겨찾기 추천. 항상 최신)
- **인터넷 없이**: 이 폴더의 `index.html` 더블클릭 (크롬 권장)

## 쓰는 법

1. 위에서 **교과서 프로필**과 **단원**을 고른다
2. 탭에서 유형을 고르고 **만들기**를 누른다
   - **빈칸 암기지** — 본문에서 단어를 무작위로 가림. 누를 때마다 새 조합. 첫 글자 힌트·해석 표시 선택 가능
   - **단어 시험** — 단원 단어장에서 무작위 출제. 영→한 / 한→영 / 섞어서
   - **영작 연습** — 해석을 보고 섞인 단어를 바른 순서로 배열
   - **시험지** — 문제은행의 객관식·서술형을 골라 시험지로 조합. 선택지 순서 섞기 가능
3. **문제지 인쇄 / 정답지 인쇄** 버튼으로 각각 출력 (A4)

## 새 교과서·단원 추가하는 법 (클로드에게 시키면 됨)

1. `data/` 아래에 프로필 폴더를 만든다 (예: `data/donga-mid2/`)
2. `data/sample-mid2/u01.js`를 본보기로 단원 파일을 만든다 — 본문 문장(en/ko), 단어장, 문제은행
3. `data/registry.js`에 프로필·단원을 등록한다
4. **`node test.js` 를 실행해 전부 통과해야 완료** — 데이터 형식·정답 인덱스·중복을 기계가 검사한다

객관식·서술형 문제은행은 클로드 코드 세션에서 본문을 주고 출제시킨다.
출제 → 별도 검증(정답 재확인·오타 점검) → 데이터 파일 반영 → `node test.js` 순서를 지킨다.

**출제 유의**: 시험지 상단에 본문 전체가 인쇄되므로, 빈칸 추론·연결어 문항은 정답 어구가
지문에 그대로 노출된다. 빈칸 유형은 지문 없이도 풀리게 발문을 구성하거나,
내용일치·제목·어법·함의 위주로 출제하는 편이 좋다. (고등 샘플 검증에서 확인된 사항)

## 주의

- 샘플 프로필의 지문은 저작권 문제가 없도록 직접 쓴 자작 글이다
- 실제 교과서 본문을 등록하면 **공개 저장소에 본문이 올라가게 된다** — 개인·가족 학습용이고 noindex(검색 차단)이지만, 저작권 이의가 오면 즉시 내리는 것을 전제로 운영
- 만든 문제지는 개인 학습용. 외부 판매·배포 금지

## 웹 배포 (2026-08-26 완료)

- 주소: **https://mycatsara.github.io/engquiz/** (GitHub Pages, 저장소 `Mycatsara/engquiz`)
- 원본은 ops-docs의 이 폴더. 수정·등록 후에는 아래 3줄로 재배포한다 (ops-docs 루트에서):

```
git branch -D engquiz-deploy 2>/dev/null; git subtree split --prefix="도구/영어문제" -b engquiz-deploy
git push -f engquiz engquiz-deploy:main
git branch -D engquiz-deploy
```

- 반영까지 1~2분. 확인은 사이트 새로고침.

## 파일 구성

| 파일 | 역할 |
|---|---|
| `index.html` / `style.css` / `app.js` | 화면 |
| `logic.js` | 빈칸·섞기·문항 선택 등 계산 로직 (화면과 분리, 테스트 대상) |
| `data/registry.js` | 프로필·단원 목록 |
| `data/<프로필>/<단원>.js` | 단원 데이터 (본문·단어·문제은행) |
| `test.js` | 자동 테스트 (`node test.js`) — 수정 후 반드시 통과시킬 것 |
